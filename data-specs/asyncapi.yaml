asyncapi: 2.5.0
info:
  title: VERA Item Table Specs
  description: |
    Data specs on tasks and items in assessments in education.
    
    Go to the Schema section of this document and select one schema. Expand all nodes and explore.

    Go to [item-table](https://iqb-berlin.github.io/item-table) to learn more about VERA data structures.
  license:
    name: CC0 1.0
    url: https://creativecommons.org/publicdomain/zero/1.0/
  version: (see schema id for version)
  contact:
    name: Institute for Educational Quality Improvement (IQB)
    url: https://www.iqb.hu-berlin.de
    email: iqb-tbadev@hu-berlin.de
channels:
  iqb_data_structures:
    subscribe:
      operationId: 'Please select one schema'
      message:
        messageId: select_schema
components:
  schemas:
    task:
      $ref: './task.schema.json'
    item:
      $ref: './item.schema.json'
    module:
      $ref: './test-module.schema.json'
    metadata-entry:
      $ref: './metadata-entry.schema.json'
    metadata-catalog:
      $ref: './metadata-catalog.schema.json'
